# Copy to .env and fill in. Never commit .env.
# Both are optional — the app runs with these defaults if .env is absent.

# Path to the SQLite index file (created on first run if missing).
SLIDEMASTER_DB_PATH=./db/slidemaster.sqlite

# Directory holding the deck .md files — the source of truth.
SLIDEMASTER_DECKS_DIR=./decks
