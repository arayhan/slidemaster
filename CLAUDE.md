# SlideMaster

**Read [AGENTS.md](AGENTS.md) first — it is the source of truth for this repo.**

This file exists only because Claude Code looks for `CLAUDE.md` by name. Every
instruction lives in `AGENTS.md` so that the same rules serve every coding agent
and cannot drift into two versions. Do not copy content here; edit `AGENTS.md`.
