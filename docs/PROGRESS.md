# SlideMaster — Progress log

Append-only decision and progress log. One line per entry: `[date] [role] [decision/what] [reason]` — roles are `pm`, `lead`, `engineer`, `reviewer`. Newest entries go at the bottom.

<!-- append entries below -->
2026-09-04 pm bootstrap-project run: filled PRODUCT/PRD/DESIGN, all docs/rules, project-gotchas. Key calls — render engine wraps @marp-team/marp-core (no own renderer); SQLite index is a pure derived view of the deck .md files (the expensive-bug invariant); config via optional SLIDEMASTER_DB_PATH / SLIDEMASTER_DECKS_DIR; starter tokens ink-black + cobalt #1B4DFF + serif headline (impeccable to refine); no formal a11y target, present-mode keyboard nav is a tested functional requirement. Reason: lock judgment content before Phase 1 build.
2026-09-16 pm Phase 1 split into 1a (design) / 1b (build); added docs/tasks/1a-gate-template.md; rewrote PRD Phase 1 DoD into runnable 1a and 1b checks and demoted the 8-decks metric to "The bet". Reason: the one master template is the whole value proposition ("zero design decisions per deck") but was scheduled as an implementation bullet next to installing better-sqlite3, and Phase 1's only DoD checkbox was a 6-12 month outcome that cannot gate a ship.
2026-09-16 ui-designer Phase 1a design pass: DESIGN.md rewritten for both surfaces, Provisional banner removed; slide/chrome token layers split and namespaced (src/theme/slide-tokens.css, src/styles/tokens.css); seven section anatomies specified with absent-field behaviour; static 10-frame mockup built at docs/references/template-mockup.html. Direction: Basel lecture poster fused with film title card, derived from the use scene (lit room, weak projector, row 12 at ~12m, 30-120s per slide) rather than category habit. Reason: the template is the product, so it is designed and signed off before any pipeline code; two surfaces are namespaced apart because slide CSS never enters the Tailwind pipeline and a class written there fails silently.
2026-09-16 pm Fixed two findings the design pass reported rather than patched: src/styles.css color-scheme was "light dark" with no dark mode in Phase 1 (dark form controls against all-light tokens); .gitignore excluded docs/references/* so the mockup, a gate deliverable, was untracked. Reason: both block 1a-gate-template review.
2026-09-16 pm Added a typecheck script and repaired two dangling references the demo-module removal (ee4e558) left behind: src/domain/index.ts re-exported ./order and ./transaction, and src/routes/index.tsx linked to the deleted /order and /transaction routes. Reason: ee4e558's body claimed "nothing dangles" on the strength of a routeTree.gen.ts grep alone; no typecheck script existed to catch the rest, which is exactly the AGENTS.md hard rule 3 failure mode — a verification claim made on a narrower check than the claim. Verified after: pnpm typecheck clean, pnpm lint clean.
2026-09-16 engineer 0-step-02-verify-loop done. pnpm test was failing for two independent reasons, and STATE.md had recorded only the second: vitest loaded vite.config.ts wholesale (no test block, no vitest.config), mounting nitro() and tanstackStart(), whose SSR machinery throws "ReferenceError: module is not defined" in vite's module-runner before any test file is discovered; and no test file existed after ee4e558. Fixed with a standalone vitest.config.ts that never loads the app plugin chain, plus vitest.config.ts added to tsconfig include so typecheck covers it. Reason: the app build and the test runner were sharing a plugin chain only one of them needs.
2026-09-16 engineer First test is src/theme/slide-tokens.test.ts — token parity between src/theme/slide-tokens.css and the mockup's TOKENS block (both directions), no raw hex or px outside that block, and all seven section types specified in DESIGN.md. Reason: 0-step-02 asks for a test of real application behavior and there is none — src/domain, lib, utils, services are empty and the real logic is Phase 1b, gated behind 1a-gate-template. Guarding the design system was the substantive thing available that does not walk through the gate. Stated as a compromise in the task file; the first real-behavior test lands in 0-step-03 or 1b. Proven able to fail before being trusted: introducing one-token drift turns it red in both directions, reverting turns it green.
2026-09-16 engineer Found that acceptance criteria 4 and 5 in 1a-step-02-template-spec.md are unsound. Their regex matches --slide-* names inside CSS comments, and slide-tokens.css:69 names one token while explaining another, so [^;]+ runs past the comment terminator and eats the real declaration after it. Both files still counted 74, so a count-based check called them identical while they were not being compared correctly. The test strips comments first; the task file still carries the broken criteria. Reason: worth recording because the failure mode is agreement for the wrong reason, which is what the file was written to catch.
2026-09-16 engineer Added pnpm typecheck as step 2 of /verify and to docs/rules/testing.md's verification table. Reason: the script was added this session and immediately caught six real errors; a check that runs once is not a check. Also filled the Tests row of docs/architecture.md's Stack table, which this step is what decides.
2026-09-17 engineer 0-step-03-data-round-trip done on feat/deck-index. Rewrote the round-trip to walk the product's real data path (decks/*.md -> reconcile -> SQLite -> server fn -> loader -> /) instead of the form-writes-a-record shape the generator's task file described. Reason: SlideMaster has no user-facing write path; docs/PRODUCT.md says deck metadata comes from frontmatter and the filesystem, and principles.md #2 says the index is a derived cache. A form writing a deck row would install the two-sources-of-truth confusion behind this project's named expensive bug.
2026-09-17 engineer Three things were silently broken before the dependency could work. pnpm-workspace.yaml allowlisted only esbuild, and pnpm 10+ blocks dependency lifecycle scripts — better-sqlite3 would have installed with no prebuild and no gyp build, failing at runtime with nothing said at install time. Local Node 24 vs CI Node 22 was a live NODE_MODULE_VERSION mismatch, and CI pnpm 9 predates allowBuilds entirely; both aligned to 24 and 11, with .nvmrc and engines added as project-gotchas had asked. And `server-only`, which AGENTS.md mandated in every src/server/ file, was never installed — the marker that works here is @tanstack/react-start/server-only.
2026-09-17 engineer The build corrected the server-only boundary. src/server/decks.ts first carried the marker and the build refused it by name. A server function exists to be called from the client and the route imports it so the handler body can be stripped, so claiming the file is server-only is a contradiction; the boundary is src/server/db/, not src/server/ wholesale. Recorded in architecture.md. The planned nitro risk did not materialise: it traces and copies the .node binary into .output/server/node_modules/ with no traceDeps config.
2026-09-17 engineer Deck-level frontmatter contract written into docs/DESIGN.md — title and topic required, id derived from the filename slug when absent, tags/speaker/logo optional, timestamps from the filesystem. Missing a required key skips the file by name rather than guessing: a filename fallback for title or a folder fallback for topic is the fabrication docs/PRODUCT.md forbids and it fails silently, while failing the whole reconcile would let one typo hide thirty good decks. Reason: the contract existed nowhere, so the parser would have invented it.
2026-09-17 engineer First tests of real application behaviour, which 0-step-02 deferred to here: 33 total, over a real temp filesystem and real temp SQLite. Five of testing.md's six named reconcile cases plus a property test asserting rows == files across forty random add/edit/delete operations. Proven able to fail before being trusted — disabling orphan deletion turns exactly four red. Also filled architecture.md's <DIAGRAM> and <FLOW> placeholders and the four <X> Stack rows, all of which were unmarked placeholders violating AGENTS.md hard rule 2, and narrowed 1a-gate-template so it gates the render pipeline and theme CSS rather than the data layer.
