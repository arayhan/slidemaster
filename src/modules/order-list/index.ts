// Example feature module -- delete this folder when real features start.
// Naming convention: docs/architecture.md > "Module file naming".
// The module's public API: only what a route needs. order-list.query.ts and
// components/ stay private, so this surface can be rearranged without callers noticing.
export { OrderListPage } from "./OrderListPage";
